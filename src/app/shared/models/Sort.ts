export class Sort {
  empty: boolean;
  sorted: boolean;
  unsorted: boolean;

  constructor(empty: boolean, sorted: boolean, unsorted: boolean) {
    this.empty = empty;
    this.sorted = sorted;
    this.unsorted = unsorted;
  }
}
